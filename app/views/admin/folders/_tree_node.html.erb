<% if !@folder.children.empty? %>
<ul id="resource_folder_<%= @folder.id %>_ul">
<% for subfolder in @folder.children%>
	<%= render :partial => 'tree_node_child', :locals => {:subfolder => subfolder}%>
<% end%>
</ul>
<% end %>